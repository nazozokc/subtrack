{
  "name": "subtrack-monorepo",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "start": "pnpm --filter subtrack start",
    "lint:types": "pnpm -r lint:types",
    "lint:security": "pnpm -r lint:security",
    "format": "nix fmt",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  }
}
